# Multi-Agent Dashboard Frontend

React + Vite + Tailwind interface for the reasoning platform.

## Run
```
cd frontend
npm install
npm run dev
```

The dev server proxies `/api` to the backend at localhost:3001.
