# Runtime data directory

Files under `backend/memory/` are runtime state (agents/config/memory/prompt versions/docs) and should not be committed.

Use the `*.example.json` files as starting points if you want to pre-seed data.

